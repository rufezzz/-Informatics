#include <fstream>

int main(void)
{
    std::ifstream in;
    std::ofstream out;
    
    in.open("input.txt");
    out.open("output.txt");
    
    int a;
    
    in>>a;
    
    int m = a%10; // ед
    int b = a%100;

    int c = 0;
    while (b>9)
    {
        b=b-10;
        c=c+1;  //дес
    }
    int l = a%1000;
    int k = 0;
    while (l>99)
    {
        l=l-100;
        k=k+1; // сот
    }
    out<<m+c+k;
    return 0;
}
///////////////////////////////
Дано трехзначное число. Найдите сумму его цифр.

Входные данные
Вводится трехзначное число.

Выходные данные
Выведите ответ на задачу.
